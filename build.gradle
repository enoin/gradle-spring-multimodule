apply plugin: AppVersionPlugin

repositories {
    mavenCentral()
    gradlePluginPortal()
}

appVersion {
    versionPropertyKey = "app.version"
    fileLocation = "web-service/src/main/resources/version.properties"
}
